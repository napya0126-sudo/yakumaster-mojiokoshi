# 薬マスタ文字起こし — 作業ルール

## 目的

内服薬マスタPDFから医薬品情報を抽出し、CSV形式で出力する。

## 出力形式

CSVの列は次の5項目とする（この順序を守ること）。

| 列名 | 内容 |
|------|------|
| 医薬品名 | 薬品名（剤形・含量を含む） |
| 効能 | 薬の作用・適応（「このお薬は、〜」など） |
| 注意事項 | 副作用・投与後の注意（「本剤投与後、〜」など） |
| 使用方法 | 与え方の指示（「獣医師の指示のもと、〜」「空腹時に与えて」など。用量行は除く） |
| 保存方法 | 保管条件（「室温で保存して下さい」など） |

- 文字コード: UTF-8（BOM付き `utf-8-sig`）
- 出力ファイル: `内服薬_マスタ.csv`

## 抽出ルール

### 1. 抽出対象

「効能」「注意事項」「使用方法」「保存方法」の4項目のうち、**1つでも記載がある**医薬品を抽出する。

### 2. 抽出対象外

上記4項目が**すべて空欄**の医薬品はCSVに含めない。

- 医薬品名だけが記載されている場合 → 対象外
- **用量の記載だけ**（「1回 ○ 錠ずつ」「1日1回」など）があり、4項目に該当する記載がない場合 → 対象外
- ページ共通の定型フッター（下記）だけの場合 → 対象外

※ 用量行は4項目の有無判定に**含めない**。4項目の記載があるかどうかだけで判断する。

### 3. 部分記載の扱い

4項目のうち記載がない項目は**空欄**とし、CSVの列構成は維持する。

例:

```csv
医薬品名,効能,注意事項,使用方法,保存方法
ｱﾋﾟﾅｯｸ錠12.5mg,このお薬は、ACE阻害剤です。...,本剤投与後、異常がみられた場合...,,
```

## 各項目の取り出し方（PDFレイアウト方式）

PDFでは項目名（効能・注意事項・使用方法・保存方法）が**選択不能な文字**で印字されている。  
抽出できるのは項目名の**横に書かれた本文だけ**。

### 基本ルール

1. 薬品名の直後から次の薬品名までが1薬品分の領域（用量行はスキップ）
2. 同一項目内の本文は**折り返しのみ**で改行される（行間が狭い）
3. **次の項目**に移るときは、行間が広く改行される
4. 項目ブロックは PDF 上の並び順に **効能 → 注意事項 → 使用方法 → 保存方法** と対応する
5. 記載のない項目は行が存在しない（CSVでは空欄）

### 技術的な判定（`extract_medicines.py`）

- `pdftotext -bbox` で各行の Y 座標を取得
- 行間の Y 座標差が **13.5pt 未満** → 同一項目内の折り返しとして結合
- 行間が **13.5pt 以上** → 次の項目ブロックの開始
- 結合されたブロックを出現順に4項目へ割り当て

※ 「1回 ○ 錠ずつ」「1日1回」などの**用量行は含めない**（4項目の判定にも使わない）

### 例

- **アトモアチュアブル25ｍｇ** … 項目ブロック1つ → 効能のみ
- **アリチア配合錠** … 折り返し3行を1ブロックに結合 → 効能のみ
- **ｱｰﾁｽﾄ錠2.5mg** … ブロック4つ → 効能・注意事項・使用方法・保存方法

## 除外するノイズ（いずれにも分類しない）

- ヘッダー: `[10000-10]`、病院名、調剤日、ページ番号
- フッター: 病院住所・電話・URL・診療時間・「処方担当」
- **ページ共通の定型文**（薬品固有の情報ではない）:
  - 「獣医師の指示のもと、用法用量を守ってお使いください。」
  - 「別の動物や人体への使用は、絶対におやめください。」

## 作業手順

1. `内服薬_*.pdf` をページ順に処理する
2. 各PDFからテキストを抽出する（`pdftotext` または `python3 extract_medicines.py`）
3. 上記ルールに従い5項目に分類する
4. `内服薬_マスタ.csv` に出力する
5. 抽出件数と、4項目すべて空の除外件数を確認する

## 品質チェック

作業完了後、次を確認すること。

- [ ] CSVヘッダーが `医薬品名,効能,注意事項,使用方法,保存方法` である
- [ ] 4項目すべて空の行が含まれていない
- [ ] 医薬品名が重複・欠落していないか（PDFの並び順を維持）
- [ ] ページフッターの定型文が各項目に混入していない
